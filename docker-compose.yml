services:

  flask:
    build: .
    container_name: flask-app
    ports:
      - "5000:5000"

    environment: 
      REDIS_HOST: redis
    depends_on:
      - redis

  redis: 
    image: redis:7-alpine
    container_name: redis-flask  
